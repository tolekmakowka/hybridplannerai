[build]
  command = ""         # statyczny serwis – nic nie buildzimy
  publish = "."

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  # exceljs jest spory – lepiej zostawić go jako zewnętrzny moduł
  external_node_modules = ["exceljs"]

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--prefer-offline --no-audit"
